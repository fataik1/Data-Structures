command = input

# if command in ['n', 's', 'e', 'w']:

commands = ['n','s','e','w', 'q', 'p']

commands[1] # constant time operation does not depend on size of the input

# accessing a dictionary value by key

rooms = {"outside": Room(...), ... }
rooms["outside"] #constant time operation does not depend on size of the input


# depends on the number of elemends in the commands list
for command in commands:
    #do something
    # adding 1 to list size increase number of loop iterations by c for some constant

for key, value in rooms.items(): #linear time operation

#print out every combination of pairs from our command list
for x in commands_1: # 6 commands
    for y in commands_2: # 10 commands
        print(x, y) # 60 prints
        (n,n), (n, s), ...
        (s,n), (s, s), ...
        (e, n), (e, s), ... 


#runtime complexity